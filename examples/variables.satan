void variables() {
    string hello = "Hello world"; // type name declaration
    const string constHello = "Hello, world!"; // Constants like in JS

    // auto type
    const numberOfTheBeast = 666;
    let sins = 7;

    // primitive types:
    // i8, i16, i32, i64, u8, u16, u32, u64 - integers
    // f64 - only float 64-bit supported
    // boolean - true or false
    // string
}